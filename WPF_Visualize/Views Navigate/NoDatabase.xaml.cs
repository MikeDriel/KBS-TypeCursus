using System.Windows.Controls;
namespace WPF_Visualize.Views_Navigate;

public partial class NoDatabase : UserControl
{
    // the screen the user sees if there is no connection to the database
    public NoDatabase()
    {
        InitializeComponent();
    }
}